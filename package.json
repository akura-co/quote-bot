{
  "name": "quoteBot",
  "version": "0.0.1",
  "description": "Telegram quotes bot",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "dependencies": {
    "underscore": "1.8.3"
  }
}
